# DEPRECATED — goreleaser is a Go-specific release tool and cannot build Rust binaries.
#
# This file has been superseded by:
#   - dist-workspace.toml           (cargo-dist configuration, binary release targets)
#   - .github/workflows/release.yml (GitHub Actions: cross-compile + GitHub Releases)
#   - .github/workflows/package.yml (GitHub Actions: multi-arch Docker image → GHCR)
#
# Do not invoke goreleaser in this repository.
